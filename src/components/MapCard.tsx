interface Props {
  name: string
  artist: string
  mapper: string
  difficulty: string
  pictureUrl: string
  key: string
  bpm: number
}

const MapCard = (props: Props) => {
  //const
}

export default MapCard
